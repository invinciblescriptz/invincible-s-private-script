-- Define the whitelist of Usernames
local whitelistedUsers = {
    "hebdjxhc",
    "krxzy_214", "alexglover020",
}

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local VirtualUser = game:GetService("VirtualUser")
local Workspace = game:GetService("Workspace")

-- Whitelist check with instant kick
if not table.find(whitelistedUsers, LocalPlayer.Name) then
    LocalPlayer:Kick("not whitelisted L bozo dont steal my shit")
    return
end

-- Load WindUI
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/main.lua"))()

local window = WindUI:CreateWindow("Invincible Paid", {
    -- You can add more options if needed (check docs)
})

-- Globals & states (unchanged)
getgenv().NexusRunning = true
local states = {
    AutoPushups = false,
    AutoWeight = false,
    AutoHandstands = false,
    FastPunch = false,
    FastStrength = false,
    InfiniteJump = false,
    AutoRebirth = false,
    AutoRebirthTarget = false,
    KillAura = false,
    AutoKillSelected = false,
    AntiAfk = false,
}

local targetRebirths = 1
local selectedTarget = nil

-- Rock farm globals (unchanged)
getgenv().rockFarmActive = false
getgenv().selectedRockDurability = 0

-- (your other globals / session timers remain the same)

-- Helper functions (unchanged — tpTo, startAutoRep, toggleFastStrength, toggleInfiniteJump, toggleAntiAfk, killPlayer)

-- UI Tabs & Sections
local mainTab       = window:CreateTab("Main")
local rebirthTab    = window:CreateTab("Rebirth")
local teleportTab   = window:CreateTab("Teleport")
local statsTab      = window:CreateTab("Stats")
local killTab       = window:CreateTab("Kill")
local miscTab       = window:CreateTab("Misc")
local creditsTab    = window:CreateTab("Credits")

local toolsSection     = mainTab:CreateSection("Tools")
local rockFarmSection  = mainTab:CreateSection("Rock Farm")
local killerSection    = killTab:CreateSection("Auto Kill")
local brawlSection     = miscTab:CreateSection("Brawl Farming")  -- if you add stuff later

-- Tool toggles (switches)
toolsSection:CreateToggle({
    Name = "Auto Pushups",
    Callback = function(s)
        states.AutoPushups = s
        if s then startAutoRep("Pushups", "AutoPushups") end
    end
})

toolsSection:CreateToggle({
    Name = "Auto Weight",
    Callback = function(s)
        states.AutoWeight = s
        if s then startAutoRep("Weight", "AutoWeight") end
    end
})

toolsSection:CreateToggle({
    Name = "Auto Handstands",
    Callback = function(s)
        states.AutoHandstands = s
        if s then startAutoRep("Handstands", "AutoHandstands") end
    end
})

toolsSection:CreateToggle({
    Name = "Auto Punch (Fast)",
    Callback = function(s)
        states.FastPunch = s
        if s then
            -- your existing fast punch loop task.spawn(...)
            task.spawn(function()
                while states.FastPunch and getgenv().NexusRunning do
                    -- (keep your original punch logic)
                    task.wait(0.09)
                end
            end)
        end
    end
})

toolsSection:CreateButton({
    Name = "Anti Lag (Boost FPS)",
    Callback = function()
        -- your existing anti-lag code
        Lighting.GlobalShadows = false
        -- ...
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end
})

toolsSection:CreateButton({
    Name = "Free AutoLift Gamepass",
    Callback = function()
        -- your existing gamepass spoof code
    end
})

-- Teleports (buttons)
teleportTab:CreateButton({Name = "To Spawn / Safe Area",     Callback = function() tpTo(Vector3.new(-6, 5, -196))   end})
teleportTab:CreateButton({Name = "To Secret Area",           Callback = function() tpTo(Vector3.new(1947, 2, 6191))  end})
teleportTab:CreateButton({Name = "To Tiny Area",             Callback = function() tpTo(Vector3.new(-39, 6, 1886))  end})
teleportTab:CreateButton({Name = "To Frozen Area",           Callback = function() tpTo(Vector3.new(-2623, 6, -409)) end})
-- ... add the rest of your teleports the same way

-- Rock Farm switches
rockFarmSection:CreateToggle({
    Name = "Farm Tiny Island Rock (0)",
    Callback = function(s) setRockFarm(0, s) end
})

rockFarmSection:CreateToggle({
    Name = "Farm Starter Island Rock (100)",
    Callback = function(s) setRockFarm(100, s) end
})

-- ... add the other rock farm toggles similarly

rockFarmSection:CreateButton({
    Name = "STOP All Rock Farming",
    Callback = function()
        getgenv().rockFarmActive = false
        getgenv().selectedRockDurability = 0
    end
})

-- Auto Kill section
killerSection:CreateToggle({
    Name = "Kill Aura (All)",
    Callback = function(s)
        states.KillAura = s
        if s then
            -- your existing kill aura loop task.spawn(...)
        end
    end
})

-- Target selection (dropdown)
local playerDropdown = killerSection:CreateDropdown({
    Name = "Select Target",
    List = {},  -- will update dynamically
    Callback = function(val)
        if val then
            selectedTarget = val:match("^([^|]+)") or val
        end
    end
})

-- Auto-update dropdown (you can improve this)
task.spawn(function()
    while true do
        local list = {}
        for _, plr in Players:GetPlayers() do
            if plr ~= LocalPlayer then
                table.insert(list, plr.Name .. " | " .. (plr.DisplayName or plr.Name))
            end
        end
        playerDropdown:SetList(list)  -- WindUI usually has SetList / UpdateList method — check exact name in docs if different
        task.wait(3)
    end
end)

killerSection:CreateToggle({
    Name = "Auto Kill Selected",
    Callback = function(s)
        states.AutoKillSelected = s
        if s and selectedTarget then
            -- your existing auto kill selected loop
        end
    end
})

-- Rebirth tab (similar conversion)
local rebirthSection = rebirthTab:CreateSection("Rebirth")

rebirthSection:CreateToggle({
    Name = "Auto Rebirth (Infinite)",
    Callback = function(s)
        states.AutoRebirth = s
        if s then autoRebirth() end
    end
})

rebirthSection:CreateInput({
    Name = "Target Rebirths",
    Placeholder = "Enter number",
    Callback = function(value)
        local num = tonumber(value)
        targetRebirths = num and num > 0 and num or 1
    end
})

rebirthSection:CreateToggle({
    Name = "Auto Rebirth to Target",
    Callback = function(s)
        states.AutoRebirthTarget = s
        if s then
            -- your existing target rebirth loop
        end
    end
})

-- Stats tab — you can add labels/paragraphs
local statsSection = statsTab:CreateSection("Player Stats")
-- Add dynamic labels if WindUI supports them (e.g. CreateLabel / CreateParagraph)
-- Example placeholder:
-- local strengthLabel = statsSection:CreateLabel("Strength: 0")
-- Then in RenderStepped update .Text

-- Keep your loops / connections / rock farm loop / etc.

print("Invincibles Priv Script – WindUI version (March 2026)")

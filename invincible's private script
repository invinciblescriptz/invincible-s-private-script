-- Define the whitelist of Usernames
local whitelistedUsers = {
    "hebdjxhc",
    "krxzy_214",
    "alexglover020",
}

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local VirtualUser = game:GetService("VirtualUser")
local Workspace = game:GetService("Workspace")

-- Whitelist check with instant kick
if not table.find(whitelistedUsers, LocalPlayer.Name) then
    LocalPlayer:Kick("not whitelisted L bozo dont steal my shit")
    return
end

-- Load Wind UI library
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/refs/heads/main/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Invincible Paid",
    -- You can add more options like Size, Theme, etc. if desired
})

-- Globals & states
getgenv().NexusRunning = true

local states = {
    AutoPushups = false,
    AutoWeight = false,
    AutoHandstands = false,
    FastPunch = false,
    FastStrength = false,
    InfiniteJump = false,
    AutoRebirth = false,
    AutoRebirthTarget = false,
    KillAura = false,
    AutoKillSelected = false,
    AntiAfk = false,
}

local targetRebirths = 1
local selectedTarget = nil

-- Rock farm globals
getgenv().rockFarmActive = false
getgenv().selectedRockDurability = 0

-- Helper functions (same as before)
local function tpTo(pos)
    pcall(function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.CFrame = CFrame.new(pos) end
    end)
end

local function startAutoRep(toolName, stateKey)
    task.spawn(function()
        while states[stateKey] and getgenv().NexusRunning do
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    task.wait(1.5)
                    return
                end
                local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
                if tool and tool.Parent ~= char then
                    char.Humanoid:EquipTool(tool)
                end
                local muscleEvent = rs:FindFirstChild("muscleEvent") -- assuming it's here
                if muscleEvent then muscleEvent:FireServer("rep") end
            end)
            task.wait(0.08)
        end
        -- unequip on stop
        pcall(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild(toolName) then
                char[toolName].Parent = LocalPlayer.Backpack
            end
        end)
    end)
end

local fastStrengthConn
local function toggleFastStrength(state)
    states.FastStrength = state
    if state then
        if fastStrengthConn then fastStrengthConn:Disconnect() end
        fastStrengthConn = RunService.Heartbeat:Connect(function()
            pcall(function()
                local muscleEvent = rs:FindFirstChild("muscleEvent")
                if muscleEvent then
                    muscleEvent:FireServer("rep")
                    muscleEvent:FireServer("punch", "rightHand")
                    muscleEvent:FireServer("punch", "leftHand")
                end
                local char = LocalPlayer.Character
                if char then
                    local tool = char:FindFirstChildWhichIsA("Tool")
                    if tool then tool:Activate() end
                end
            end)
        end)
    else
        if fastStrengthConn then fastStrengthConn:Disconnect() fastStrengthConn = nil end
    end
end

local jumpConn
local function toggleInfiniteJump(state)
    states.InfiniteJump = state
    if state then
        if jumpConn then return end
        jumpConn = UserInputService.JumpRequest:Connect(function()
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end)
        end)
    else
        if jumpConn then jumpConn:Disconnect() jumpConn = nil end
    end
end

local antiAfkConn
local function toggleAntiAfk(state)
    states.AntiAfk = state
    if state then
        if antiAfkConn then return end
        antiAfkConn = LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end
    end
end

local function killPlayer(target)
    pcall(function()
        local myChar = LocalPlayer.Character
        local tChar = target.Character
        if not myChar or not tChar then return end
        local myHRP = myChar:FindFirstChild("HumanoidRootPart")
        local tHRP = tChar:FindFirstChild("HumanoidRootPart")
        if myHRP and tHRP then
            myHRP.CFrame = tHRP.CFrame * CFrame.new(0, -25, 0)
        end
    end)
end

-- ──────────────────────────────────────────────────────────────
-- Tabs & Sections
-- ──────────────────────────────────────────────────────────────

local MainTab     = Window:CreateTab({ Title = "Main" })
local RebirthTab  = Window:CreateTab({ Title = "Rebirth" })
local TeleportTab = Window:CreateTab({ Title = "Teleport" })
local KillTab     = Window:CreateTab({ Title = "Kill" })
local MiscTab     = Window:CreateTab({ Title = "Misc" })
local CreditsTab  = Window:CreateTab({ Title = "Credits" })

local ToolsSection    = MainTab:CreateSection({ Title = "Tools" })
local RockFarmSection = MainTab:CreateSection({ Title = "Rock Farm" })
local AutoKillSection = KillTab:CreateSection({ Title = "Auto Kill" })
local BrawlSection    = MiscTab:CreateSection({ Title = "Brawl Farming" }) -- if you want to expand later

-- ──────────────────────────────────────────────────────────────
-- Tools toggles
-- ──────────────────────────────────────────────────────────────

ToolsSection:CreateToggle({
    Title = "Auto Pushups",
    Callback = function(value)
        states.AutoPushups = value
        if value then startAutoRep("Pushups", "AutoPushups") end
    end
})

ToolsSection:CreateToggle({
    Title = "Auto Weight",
    Callback = function(value)
        states.AutoWeight = value
        if value then startAutoRep("Weight", "AutoWeight") end
    end
})

ToolsSection:CreateToggle({
    Title = "Auto Handstands",
    Callback = function(value)
        states.AutoHandstands = value
        if value then startAutoRep("Handstands", "AutoHandstands") end
    end
})

ToolsSection:CreateToggle({
    Title = "Fast Strength (rep + punch spam)",
    Callback = toggleFastStrength
})

ToolsSection:CreateToggle({
    Title = "Infinite Jump",
    Callback = toggleInfiniteJump
})

ToolsSection:CreateToggle({
    Title = "Anti AFK",
    Callback = toggleAntiAfk
})

-- ──────────────────────────────────────────────────────────────
-- Buttons
-- ──────────────────────────────────────────────────────────────

ToolsSection:CreateButton({
    Title = "Anti Lag (Boost FPS)",
    Callback = function()
        Lighting.GlobalShadows = false
        Lighting.Brightness = 1
        Lighting.ClockTime = 12
        Lighting.FogEnd = 9999
        Lighting.FogStart = 0
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
               v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
                v.Enabled = false
            end
        end
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end
})

ToolsSection:CreateButton({
    Title = "Free AutoLift Gamepass",
    Callback = function()
        pcall(function()
            local gpFolder = rs:FindFirstChild("gamepassIds")
            if gpFolder then
                local owned = LocalPlayer:FindFirstChild("ownedGamepasses") or Instance.new("Folder")
                owned.Name = "ownedGamepasses"
                owned.Parent = LocalPlayer
                for _, gp in pairs(gpFolder:GetChildren()) do
                    local val = Instance.new("IntValue")
                    val.Name = gp.Name
                    val.Value = gp.Value
                    val.Parent = owned
                end
            end
        end)
    end
})

-- ──────────────────────────────────────────────────────────────
-- Teleports
-- ──────────────────────────────────────────────────────────────

TeleportTab:CreateButton({ Title = "To Spawn / Safe Area",     Callback = function() tpTo(Vector3.new(-6, 5, -196))    end })
TeleportTab:CreateButton({ Title = "To Secret Area",            Callback = function() tpTo(Vector3.new(1947, 2, 6191))  end })
TeleportTab:CreateButton({ Title = "To Tiny Area",              Callback = function() tpTo(Vector3.new(-39, 6, 1886))   end })
TeleportTab:CreateButton({ Title = "To Frozen Area",            Callback = function() tpTo(Vector3.new(-2623, 6, -409))  end })
TeleportTab:CreateButton({ Title = "To Mythical Area",          Callback = function() tpTo(Vector3.new(2251, 6, 1073))   end })
TeleportTab:CreateButton({ Title = "To Inferno Area",           Callback = function() tpTo(Vector3.new(-6759, 6, -1285)) end })
TeleportTab:CreateButton({ Title = "To Legend Area",            Callback = function() tpTo(Vector3.new(4603, 990, -3898)) end })
TeleportTab:CreateButton({ Title = "To Muscle King Area",       Callback = function() tpTo(Vector3.new(-8626, 16, -5730)) end })
TeleportTab:CreateButton({ Title = "To Jungle Area",            Callback = function() tpTo(Vector3.new(-8642, 6, 2381))   end })
TeleportTab:CreateButton({ Title = "To Lava Brawl",             Callback = function() tpTo(Vector3.new(4471, 119, -8836)) end })
TeleportTab:CreateButton({ Title = "To Desert Brawl",           Callback = function() tpTo(Vector3.new(960, 17, -7398))  end })
TeleportTab:CreateButton({ Title = "To Beach Brawl",            Callback = function() tpTo(Vector3.new(-1849, 20, -6335)) end })

-- ──────────────────────────────────────────────────────────────
-- Rock Farm
-- ──────────────────────────────────────────────────────────────

local function setRockFarm(dur, enabled)
    if enabled then
        getgenv().selectedRockDurability = dur
        getgenv().rockFarmActive = true
    else
        if getgenv().selectedRockDurability == dur then
            getgenv().rockFarmActive = false
            getgenv().selectedRockDurability = 0
        end
    end
end

RockFarmSection:CreateToggle({
    Title = "Farm Tiny Island Rock (0)",
    Callback = function(v) setRockFarm(0, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Starter Island Rock (100)",
    Callback = function(v) setRockFarm(100, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Legend Beach Rock (5000)",
    Callback = function(v) setRockFarm(5000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Frost Gym Rock (150k)",
    Callback = function(v) setRockFarm(150000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Mythical Gym Rock (400k)",
    Callback = function(v) setRockFarm(400000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Eternal Gym Rock (750k)",
    Callback = function(v) setRockFarm(750000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Legend Gym Rock (1M)",
    Callback = function(v) setRockFarm(1000000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Muscle King Gym Rock (5M)",
    Callback = function(v) setRockFarm(5000000, v) end
})
RockFarmSection:CreateToggle({
    Title = "Farm Ancient Jungle Rock (10M)",
    Callback = function(v) setRockFarm(10000000, v) end
})

RockFarmSection:CreateButton({
    Title = "STOP All Rock Farming",
    Callback = function()
        getgenv().rockFarmActive = false
        getgenv().selectedRockDurability = 0
    end
})

-- Rock farm loop (unchanged)
task.spawn(function()
    while getgenv().NexusRunning do
        if not getgenv().rockFarmActive or getgenv().selectedRockDurability <= 0 then
            task.wait(0.6)
            continue
        end
        pcall(function()
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                task.wait(1.2)
                return
            end
            local dur = LocalPlayer:FindFirstChild("Durability")
            if not dur or dur.Value < getgenv().selectedRockDurability then
                task.wait(0.5)
                return
            end
            local hum = char:FindFirstChild("Humanoid")
            local punch = char:FindFirstChild("Punch") or LocalPlayer.Backpack:FindFirstChild("Punch")
            if hum and punch and punch.Parent ~= char then hum:EquipTool(punch) end

            local touched = false
            for _, obj in pairs(Workspace.machinesFolder:GetDescendants()) do
                if obj.Name == "neededDurability" and obj:IsA("IntValue") and obj.Value == getgenv().selectedRockDurability then
                    local rock = obj.Parent:FindFirstChild("Rock")
                    if rock and rock:IsA("BasePart") then
                        touched = true
                        if char:FindFirstChild("RightHand") then
                            firetouchinterest(rock, char.RightHand, 0) task.wait(0.025) firetouchinterest(rock, char.RightHand, 1)
                        end
                        if char:FindFirstChild("LeftHand") then
                            firetouchinterest(rock, char.LeftHand, 0) task.wait(0.025) firetouchinterest(rock, char.LeftHand, 1)
                        end
                    end
                end
            end
            local muscleEvent = rs:FindFirstChild("muscleEvent")
            if touched and muscleEvent then
                muscleEvent:FireServer("punch", "leftHand")
                muscleEvent:FireServer("punch", "rightHand")
            end
        end)
        task.wait(0.15)
    end
end)

-- ──────────────────────────────────────────────────────────────
-- Kill Aura & Target Kill
-- ──────────────────────────────────────────────────────────────

KillTab:CreateToggle({
    Title = "Kill Aura (All players)",
    Callback = function(enabled)
        states.KillAura = enabled
        if enabled then
            task.spawn(function()
                while states.KillAura and getgenv().NexusRunning do
                    for _, plr in ipairs(Players:GetPlayers()) do
                        if plr ~= LocalPlayer then killPlayer(plr) end
                    end
                    task.wait(0.12)
                end
            end)
        end
    end
})

-- Target selection (dropdown)
local playerList = {}
local function refreshPlayerList()
    playerList = {}
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            table.insert(playerList, plr.Name)
        end
    end
end

refreshPlayerList()
Players.PlayerAdded:Connect(refreshPlayerList)
Players.PlayerRemoving:Connect(refreshPlayerList)

local targetDropdown = KillTab:CreateDropdown({
    Title = "Select Target",
    Items = playerList,
    Callback = function(selected)
        selectedTarget = selected
    end
})

-- Auto-refresh dropdown every few seconds
task.spawn(function()
    while true do
        refreshPlayerList()
        -- WindUI might not have direct :SetItems, so you may need to recreate or check docs
        -- For simplicity we skip live update here or implement differently if library supports
        task.wait(5)
    end
end)

KillTab:CreateToggle({
    Title = "Auto Kill Selected Player",
    Callback = function(enabled)
        states.AutoKillSelected = enabled
        if enabled and selectedTarget then
            task.spawn(function()
                while states.AutoKillSelected and getgenv().NexusRunning do
                    local plr = Players:FindFirstChild(selectedTarget)
                    if plr then killPlayer(plr) end
                    task.wait(0.12)
                end
            end)
        end
    end
})

-- ──────────────────────────────────────────────────────────────
-- Rebirth tab
-- ──────────────────────────────────────────────────────────────

local RebirthSection = RebirthTab:CreateSection({ Title = "Rebirth Controls" })

RebirthSection:CreateToggle({
    Title = "Auto Rebirth (Infinite)",
    Callback = function(enabled)
        states.AutoRebirth = enabled
        if enabled then
            task.spawn(function()
                while states.AutoRebirth and getgenv().NexusRunning do
                    pcall(function()
                        local rEvents = rs:FindFirstChild("rEvents")
                        if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                            rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                        end
                    end)
                    task.wait(0.1)
                end
            end)
        end
    end
})

RebirthSection:CreateInput({
    Title = "Target Rebirths",
    Placeholder = "Enter number",
    Callback = function(value)
        local num = tonumber(value)
        targetRebirths = num and num > 0 and num or 1
    end
})

RebirthSection:CreateToggle({
    Title = "Auto Rebirth until Target",
    Callback = function(enabled)
        states.AutoRebirthTarget = enabled
        if enabled then
            task.spawn(function()
                while states.AutoRebirthTarget and getgenv().NexusRunning do
                    pcall(function()
                        local rebirths = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Rebirths")
                        if rebirths and rebirths.Value >= targetRebirths then
                            states.AutoRebirthTarget = false
                            return
                        end
                        local rEvents = rs:FindFirstChild("rEvents")
                        if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                            rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                        end
                    end)
                    task.wait(0.12)
                end
            end)
        end
    end
})

-- Credits tab (simple paragraph example)
CreditsTab:CreateParagraph({
    Title = "Credits",
    Content = "Invincibles Priv Script – fixed & organized (Feb/Mar 2026)\nUI ported to WindUI"
})

print("Invincible Paid – now using Wind UI")

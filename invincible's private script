-- Invincibles Priv Script – cleaned & safer (March 2026 version)
-- Updated UI to Fluent, added delays for safety, assumed remotes are similar (test and replace if changed)

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = string.format("invincible private | Hello %s", game.Players.LocalPlayer.DisplayName),
    SubTitle = "March 2026",
    TabWidth = 160,
    Size = UDim2.fromOffset(660, 870),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local muscleEvent = LocalPlayer:WaitForChild("muscleEvent", 8) -- Check if still exists, else find new

-- Globals & states
getgenv().NexusRunning = true

local states = {
    AutoPushups       = false,
    AutoWeight        = false,
    AutoHandstands    = false,
    FastPunch         = false,
    FastStrength      = false,
    FastRebirth       = false,
    InfiniteJump      = false,
    AntiAfk           = false,
    AutoRebirth       = false,
    AutoRebirthTarget = false,
    KillAura          = false,
    AutoKillSelected  = false,
}

local targetRebirths   = 1
local selectedTarget   = nil
local fastStrengthConn = nil
local jumpConn         = nil
local antiAfkConn      = nil

-- Rock farm globals
getgenv().rockFarmActive         = false
getgenv().selectedRockDurability = 0

-- =============================================================
-- Helpers
-- =============================================================

local function tpTo(pos)
    pcall(function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(pos)
        end
    end)
end

local function safeEquipTool(toolName)
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Humanoid") then return end
        local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
        if tool and tool.Parent ~= char then
            char.Humanoid:EquipTool(tool)
        end
    end)
end

local function startAutoRep(toolName, stateKey)
    task.spawn(function()
        while states[stateKey] and getgenv().NexusRunning do
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    task.wait(1.6) return
                end
                safeEquipTool(toolName)
                if muscleEvent then
                    muscleEvent:FireServer("rep")
                end
            end)
            task.wait(0.15) -- Increased delay for safety
        end
        -- Unequip when disabled
        pcall(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild(toolName) then
                char[toolName].Parent = LocalPlayer.Backpack
            end
        end)
    end)
end

-- =============================================================
-- Fast Strength (combined grind)
-- =============================================================

local function toggleFastStrength(state)
    states.FastStrength = state
    if state then
        if fastStrengthConn then fastStrengthConn:Disconnect() end
        fastStrengthConn = RunService.Heartbeat:Connect(function()
            pcall(function()
                if not muscleEvent then return end
                muscleEvent:FireServer("rep")
                muscleEvent:FireServer("punch", "rightHand")
                muscleEvent:FireServer("punch", "leftHand")

                local char = LocalPlayer.Character
                if char then
                    local tool = char:FindFirstChildWhichIsA("Tool")
                    if tool then tool:Activate() end
                end
            end)
            task.wait(0.12) -- Delay inside loop
        end)
    else
        if fastStrengthConn then
            fastStrengthConn:Disconnect()
            fastStrengthConn = nil
        end
    end
end

-- =============================================================
-- Infinite Jump
-- =============================================================

local function toggleInfiniteJump(state)
    states.InfiniteJump = state
    if state then
        if jumpConn then return end
        jumpConn = UserInputService.JumpRequest:Connect(function()
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        end)
    else
        if jumpConn then
            jumpConn:Disconnect()
            jumpConn = nil
        end
    end
end

-- =============================================================
-- Anti-AFK (2026 safe method)
-- =============================================================

local function toggleAntiAfk(state)
    states.AntiAfk = state
    if state then
        if antiAfkConn then return end
        antiAfkConn = LocalPlayer.Idled:Connect(function()
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        end)
    else
        if antiAfkConn then
            antiAfkConn:Disconnect()
            antiAfkConn = nil
        end
    end
end

-- =============================================================
-- Kill / Position under target
-- =============================================================

local function killPlayer(targetPlayer)
    pcall(function()
        local myChar = LocalPlayer.Character
        local tChar  = targetPlayer.Character
        if not myChar or not tChar then return end

        local myHRP = myChar:FindFirstChild("HumanoidRootPart")
        local tHRP  = tChar:FindFirstChild("HumanoidRootPart")
        if myHRP and tHRP then
            myHRP.CFrame = tHRP.CFrame * CFrame.new(0, -25, 0)
        end
    end)
end

-- =============================================================
-- Tabs (using Fluent)
-- =============================================================

local mainTab = Window:AddTab({Title = "Main", Icon = ""})
local farmingTab = Window:AddTab({Title = "Better Farming", Icon = ""})
local rebirthTab = Window:AddTab({Title = "Rebirth", Icon = ""})
local teleportTab = Window:AddTab({Title = "Teleport", Icon = ""})
local killerTab = Window:AddTab({Title = "Killer", Icon = ""})
local miscTab = Window:AddTab({Title = "Misc", Icon = ""})

-- Tools
mainTab:AddToggle("AutoPushups", {Text = "Auto Pushups", Default = false, Callback = function(s) 
    states.AutoPushups = s 
    if s then startAutoRep("Pushups", "AutoPushups") end 
end})
mainTab:AddToggle("AutoWeight", {Text = "Auto Weight", Default = false, Callback = function(s) 
    states.AutoWeight = s 
    if s then startAutoRep("Weight", "AutoWeight") end 
end})
mainTab:AddToggle("AutoHandstands", {Text = "Auto Handstands", Default = false, Callback = function(s) 
    states.AutoHandstands = s 
    if s then startAutoRep("Handstands", "AutoHandstands") end 
end})

mainTab:AddToggle("FastPunch", {Text = "Auto Punch (Fast)", Default = false, Callback = function(s)
    states.FastPunch = s
    if s then
        task.spawn(function()
            while states.FastPunch and getgenv().NexusRunning do
                pcall(function()
                    if muscleEvent then
                        muscleEvent:FireServer("punch", "rightHand")
                        muscleEvent:FireServer("punch", "leftHand")
                    end
                    local char = LocalPlayer.Character
                    if char then
                        local punch = char:FindFirstChild("Punch") or LocalPlayer.Backpack:FindFirstChild("Punch")
                        if punch and punch.Parent ~= char then
                            char.Humanoid:EquipTool(punch)
                        end
                        if punch and punch.Parent == char then
                            local atk = punch:FindFirstChild("attackTime")
                            if atk and atk:IsA("NumberValue") then atk.Value = 0.01 end
                            punch:Activate()
                        end
                    end
                end)
                task.wait(0.12) -- Safety delay
            end
        end)
    end
end})

mainTab:AddButton({Title = "Anti Lag (Boost FPS)", Callback = function()
    local Lighting = game:GetService("Lighting")
    Lighting.GlobalShadows = false
    Lighting.Brightness = 1
    Lighting.ClockTime = 12
    Lighting.FogEnd = 9999
    Lighting.FogStart = 0

    for _, v in pairs(Workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
           v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = false
        end
    end

    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
end})

mainTab:AddButton({Title = "Free AutoLift Gamepass", Callback = function()
    pcall(function()
        local gpFolder = ReplicatedStorage:FindFirstChild("gamepassIds")
        if not gpFolder then return end

        local owned = LocalPlayer:FindFirstChild("ownedGamepasses") or Instance.new("Folder")
        owned.Name = "ownedGamepasses"
        owned.Parent = LocalPlayer

        for _, gp in pairs(gpFolder:GetChildren()) do
            if gp:IsA("IntValue") then
                local val = Instance.new("IntValue")
                val.Name = gp.Name
                val.Value = gp.Value
                val.Parent = owned
            end
        end
    end)
end})

-- Teleport Tab
teleportTab:AddButton({Title = "To Spawn / Safe Area", Callback = function() tpTo(Vector3.new(-6, 5, -196)) end})
teleportTab:AddButton({Title = "To Secret Area", Callback = function() tpTo(Vector3.new(1947, 2, 6191)) end})
teleportTab:AddButton({Title = "To Tiny Area", Callback = function() tpTo(Vector3.new(-39, 6, 1886)) end})
teleportTab:AddButton({Title = "To Frozen Area", Callback = function() tpTo(Vector3.new(-2623, 6, -409)) end})
teleportTab:AddButton({Title = "To Mythical Area", Callback = function() tpTo(Vector3.new(2251, 6, 1073)) end})
teleportTab:AddButton({Title = "To Inferno Area", Callback = function() tpTo(Vector3.new(-6759, 6, -1285)) end})
teleportTab:AddButton({Title = "To Legend Area", Callback = function() tpTo(Vector3.new(4603, 990, -3898)) end})
teleportTab:AddButton({Title = "To Muscle King Area", Callback = function() tpTo(Vector3.new(-8626, 16, -5730)) end})
teleportTab:AddButton({Title = "To Jungle Area", Callback = function() tpTo(Vector3.new(-8642, 6, 2381)) end})
teleportTab:AddButton({Title = "To Lava Brawl", Callback = function() tpTo(Vector3.new(4471, 119, -8836)) end})
teleportTab:AddButton({Title = "To Desert Brawl", Callback = function() tpTo(Vector3.new(960, 17, -7398)) end})
teleportTab:AddButton({Title = "To Beach Brawl", Callback = function() tpTo(Vector3.new(-1849, 20, -6335)) end})

-- Rock Farm
local rockFarmSection = mainTab:AddSection({Title = "Rock Farm"})

local function setRockFarm(dur, enable)
    if enable then
        getgenv().selectedRockDurability = dur
        getgenv().rockFarmActive = true
    else
        if getgenv().selectedRockDurability == dur then
            getgenv().rockFarmActive = false
            getgenv().selectedRockDurability = 0
        end
    end
end

rockFarmSection:AddToggle("FarmTiny0", {Text = "Farm Tiny Island Rock (0)", Default = false, Callback = function(s) setRockFarm(0, s) end})
rockFarmSection:AddToggle("FarmStarter100", {Text = "Farm Starter Island Rock (100)", Default = false, Callback = function(s) setRockFarm(100, s) end})
rockFarmSection:AddToggle("FarmLegend5000", {Text = "Farm Legend Beach Rock (5000)", Default = false, Callback = function(s) setRockFarm(5000, s) end})
rockFarmSection:AddToggle("FarmFrost150k", {Text = "Farm Frost Gym Rock (150k)", Default = false, Callback = function(s) setRockFarm(150000, s) end})
rockFarmSection:AddToggle("FarmMythical400k", {Text = "Farm Mythical Gym Rock (400k)", Default = false, Callback = function(s) setRockFarm(400000, s) end})
rockFarmSection:AddToggle("FarmEternal750k", {Text = "Farm Eternal Gym Rock (750k)", Default = false, Callback = function(s) setRockFarm(750000, s) end})
rockFarmSection:AddToggle("FarmLegend1M", {Text = "Farm Legend Gym Rock (1M)", Default = false, Callback = function(s) setRockFarm(1000000, s) end})
rockFarmSection:AddToggle("FarmMuscleKing5M", {Text = "Farm Muscle King Gym Rock (5M)", Default = false, Callback = function(s) setRockFarm(5000000, s) end})
rockFarmSection:AddToggle("FarmJungle10M", {Text = "Farm Ancient Jungle Rock (10M)", Default = false, Callback = function(s) setRockFarm(10000000, s) end})

rockFarmSection:AddButton({Title = "STOP All Rock Farming", Callback = function()
    getgenv().rockFarmActive = false
    getgenv().selectedRockDurability = 0
end})

-- Rock farm loop
task.spawn(function()
    while getgenv().NexusRunning do
        if not getgenv().rockFarmActive or getgenv().selectedRockDurability <= 0 then
            task.wait(0.8) continue
        end

        pcall(function()
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                task.wait(1.4) return
            end

            local dur = LocalPlayer:FindFirstChild("Durability")
            if not dur or dur.Value < getgenv().selectedRockDurability then
                task.wait(0.6) return
            end

            safeEquipTool("Punch")

            for _, obj in pairs(Workspace.machinesFolder:GetDescendants()) do
                if obj.Name == "neededDurability" and obj:IsA("IntValue") and obj.Value == getgenv().selectedRockDurability then
                    local rock = obj.Parent:FindFirstChild("Rock")
                    if rock and rock:IsA("BasePart") then
                        if char:FindFirstChild("RightHand") then
                            firetouchinterest(rock, char.RightHand, 0)
                            task.wait(0.05)
                            firetouchinterest(rock, char.RightHand, 1)
                        end
                        if char:FindFirstChild("LeftHand") then
                            firetouchinterest(rock, char.LeftHand, 0)
                            task.wait(0.05)
                            firetouchinterest(rock, char.LeftHand, 1)
                        end
                        if muscleEvent then
                            muscleEvent:FireServer("punch", "leftHand")
                            muscleEvent:FireServer("punch", "rightHand")
                        end
                    end
                end
            end
        end)

        task.wait(0.25) -- Increased for safety
    end
end)

-- Killer Tab
killerTab:AddToggle("KillAura", {Text = "Kill Aura (All)", Default = false, Callback = function(s)
    states.KillAura = s
    if s then
        task.spawn(function()
            while states.KillAura and getgenv().NexusRunning do
                for _, plr in Players:GetPlayers() do
                    if plr ~= LocalPlayer and plr.Character then
                        killPlayer(plr)
                    end
                end
                task.wait(0.3) -- Slower for safety
            end
        end)
    end
end})

local playerOptions = {}
for _, plr in Players:GetPlayers() do
    if plr ~= LocalPlayer then
        table.insert(playerOptions, plr.Name)
    end
end
local playerDropdown = killerTab:AddDropdown("SelectTarget", {Title = "Select Target", Values = playerOptions, Multi = false, Default = 1, Callback = function(val)
    selectedTarget = val
end})

killerTab:AddToggle("AutoKillSelected", {Text = "Auto Kill Selected", Default = false, Callback = function(s)
    states.AutoKillSelected = s
    if s and selectedTarget then
        task.spawn(function()
            while states.AutoKillSelected and getgenv().NexusRunning do
                local plr = Players:FindFirstChild(selectedTarget)
                if plr and plr.Character then
                    killPlayer(plr)
                end
                task.wait(0.3)
            end
        end)
    end
end})

-- Auto-update player list
task.spawn(function()
    while getgenv().NexusRunning do
        playerOptions = {}
        for _, plr in Players:GetPlayers() do
            if plr ~= LocalPlayer then
                table.insert(playerOptions, plr.Name)
            end
        end
        playerDropdown:SetValues(playerOptions)
        task.wait(6)
    end
end)

-- Better Farming
farmingTab:AddToggle("FastStrength", {Text = "Fast Grind / Strength", Default = false, Callback = toggleFastStrength})

farmingTab:AddToggle("FastRebirth", {Text = "Fast Rebirth", Default = false, Callback = function(s)
    states.FastRebirth = s
    if s then
        task.spawn(function()
            while states.FastRebirth and getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.25) -- Slower rate
            end
        end)
    end
end})

-- Misc
miscTab:AddToggle("InfiniteJump", {Text = "Infinite Jump", Default = false, Callback = toggleInfiniteJump})
miscTab:AddToggle("AntiAfk", {Text = "Anti-AFK", Default = false, Callback = toggleAntiAfk})

-- Rebirth Tab
rebirthTab:AddToggle("AutoRebirthInfinite", {Text = "Auto Rebirth (Infinite)", Default = false, Callback = function(s)
    states.AutoRebirth = s
    if s then
        task.spawn(function()
            while states.AutoRebirth and getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end})

rebirthTab:AddInput("TargetRebirths", {Title = "Target Rebirths", Default = "1", Callback = function(value)
    local num = tonumber(value)
    targetRebirths = num and num > 0 and num or 1
end})

rebirthTab:AddToggle("AutoRebirthTarget", {Text = "Auto Rebirth to Target", Default = false, Callback = function(s)
    states.AutoRebirthTarget = s
    if s then
        task.spawn(function()
            while states.AutoRebirthTarget and getgenv().NexusRunning do
                pcall(function()
                    local rebirths = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Rebirths")
                    if rebirths and rebirths.Value >= targetRebirths then
                        states.AutoRebirthTarget = false
                        return
                    end
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end})

rebirthTab:AddToggle("AutoSize2", {Text = "Auto Size 2", Default = false, Callback = function(s)
    if s then
        getgenv().sizeLoop = task.spawn(function()
            while getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("changeSpeedSizeRemote") then
                        rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 2)
                    end
                end)
                task.wait(0.15)
            end
        end)
    else
        if getgenv().sizeLoop then
            task.cancel(getgenv().sizeLoop)
            getgenv().sizeLoop = nil
        end
    end
end})

rebirthTab:AddToggle("AutoTPMuscleKing", {Text = "Auto TP Muscle King", Default = false, Callback = function(s)
    if s then
        getgenv().kingTpLoop = task.spawn(function()
            while getgenv().NexusRunning do
                tpTo(Vector3.new(-8646, 17, -5738))
                task.wait(0.25)
            end
        end)
    else
        if getgenv().kingTpLoop then
            task.cancel(getgenv().kingTpLoop)
            getgenv().kingTpLoop = nil
        end
    end
end})

print("Invincibles Priv Script – cleaned & safer (March 2026)")

-- Define the whitelist of Usernames
local whitelistedUsers = {
    "hebdjxhc",
    "krxzy_214", "alexglover020",
}
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local VirtualUser = game:GetService("VirtualUser")
local Workspace = game:GetService("Workspace")

-- Whitelist check with instant kick
if not table.find(whitelistedUsers, LocalPlayer.Name) then
    LocalPlayer:Kick("not whitelisted L bozo dont steal my shit")
    return
end

-- Load Visual UI Library
local Library = loadstring(game:HttpGet('https://raw.githubusercontent.com/VisualRoblox/Roblox/main/UI-Libraries/Visual%20UI%20Library/Source.lua'))()

-- Create main window with intro
local window = Library:CreateWindow(
    "Invincible Paid",              -- HubName
    "Strength Simulator",           -- GameName (customize)
    "Welcome to Invincible Paid",   -- IntroText
    "rbxassetid://0",               -- IntroIcon (replace with real asset id if wanted)
    false,                          -- ImprovePerformance
    "InvincibleConfigs",            -- ConfigFolder
    "Default"                       -- Theme (or "Lighter", "Nordic Dark", etc.)
)

-- Define remotes safely
local muscleEvent = rs:WaitForChild("muscleEvent", 8)
local rEvents = rs:WaitForChild("rEvents", 8)

-- Globals & states (unchanged)
getgenv().NexusRunning = true
local states = {
    AutoPushups = false,
    AutoWeight = false,
    AutoHandstands = false,
    FastPunch = false,
    FastStrength = false,
    InfiniteJump = false,
    AutoRebirth = false,
    AutoRebirthTarget = false,
    KillAura = false,
    AutoKillSelected = false,
    AntiAfk = false,
}
local targetRebirths = 1
local selectedTarget = nil

-- Rock farm globals
getgenv().rockFarmActive = false
getgenv().selectedRockDurability = 0

-- Session timer
local sessionStartTime = os.time()

-- Utility functions (unchanged)
local function tpTo(pos)
    pcall(function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.CFrame = CFrame.new(pos) end
    end)
end

local function formatNumber(number)
    local formatted = tostring(math.floor(number))
    while true do
        local newFormatted, k = string.gsub(formatted, "^(-?%d+)(%d%d%d)", "%1,%2")
        if k == 0 then break end
        formatted = newFormatted
    end
    return formatted
end

local function formatTime(seconds)
    local days = math.floor(seconds / 86400)
    local hours = math.floor((seconds % 86400) / 3600)
    local minutes = math.floor((seconds % 3600) / 60)
    local secs = seconds % 60
    return string.format("%dd %dh %dm %ds", days, hours, minutes, secs)
end

-- [Your helper functions: startAutoRep, toggleFastStrength, toggleInfiniteJump, toggleAntiAfk, killPlayer remain unchanged]
-- ... (paste them here from your original script)

-- UI Tabs & Sections
local mainTab = window:CreateTab("Main", true)          -- true = default visible
local rebirthTab = window:CreateTab("Rebirth")
local teleportTab = window:CreateTab("Teleport")
local statsTab = window:CreateTab("Stats")
local killTab = window:CreateTab("Kill")
local miscTab = window:CreateTab("Misc")
local creditsTab = window:CreateTab("Credits")

local toolsSection = mainTab:CreateSection("Tools")
local rockSection = mainTab:CreateSection("Rock Farm")
local killSection = killTab:CreateSection("Auto Kill")
local brawlSection = miscTab:CreateSection("Brawl Farming")

-- Tools toggles
toolsSection:CreateToggle("Fast Strength", false, function(state)
    toggleFastStrength(state)
end)

toolsSection:CreateToggle("Infinite Jump", false, function(state)
    toggleInfiniteJump(state)
end)

toolsSection:CreateToggle("Anti-AFK", false, function(state)
    toggleAntiAfk(state)
end)

toolsSection:CreateToggle("Auto Pushups", false, function(s)
    states.AutoPushups = s
    if s then startAutoRep("Pushups", "AutoPushups") end
end)

toolsSection:CreateToggle("Auto Weight", false, function(s)
    states.AutoWeight = s
    if s then startAutoRep("Weight", "AutoWeight") end
end)

toolsSection:CreateToggle("Auto Handstands", false, function(s)
    states.AutoHandstands = s
    if s then startAutoRep("Handstands", "AutoHandstands") end
end)

toolsSection:CreateToggle("Auto Punch (Fast)", false, function(s)
    states.FastPunch = s
    if s then
        -- [your existing FastPunch loop task.spawn here]
    end
end)

toolsSection:CreateButton("Anti Lag (Boost FPS)", function()
    -- [your existing anti-lag code here]
end)

toolsSection:CreateButton("Free AutoLift Gamepass", function()
    -- [your existing gamepass fake code here]
end)

-- Teleports as buttons
local tpSection = teleportTab:CreateSection("Areas")
tpSection:CreateButton("To Spawn / Safe Area", function() tpTo(Vector3.new(-6, 5, -196)) end)
-- [add all other teleport buttons similarly]

-- Rock farm switches
rockSection:CreateToggle("Farm Tiny Island Rock (0)", false, function(s) setRockFarm(0, s) end)
-- [add all other rock toggles similarly]

rockSection:CreateButton("STOP All Rock Farming", function()
    getgenv().rockFarmActive = false
    getgenv().selectedRockDurability = 0
end)

-- Kill aura & target
killSection:CreateToggle("Kill Aura (All)", false, function(s)
    states.KillAura = s
    if s then
        -- [your kill aura loop]
    end
end)

local targetDropdown = killSection:CreateDropdown("Select Target", {}, nil, 0.25, function(val)
    selectedTarget = val
end)

local function updateDropdown()
    local list = {}
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            table.insert(list, plr.Name .. " | " .. (plr.DisplayName or plr.Name))
        end
    end
    targetDropdown:UpdateDropdown(list)
end

task.spawn(function()
    while true do
        updateDropdown()
        task.wait(2)
    end
end)

killSection:CreateToggle("Auto Kill Selected", false, function(s)
    states.AutoKillSelected = s
    if s and selectedTarget then
        -- [your auto kill loop]
    end
end)

-- Stats tab
local statsSection = statsTab:CreateSection("Player Stats")
local sessionLabel = statsSection:CreateParagraph("Session Time", "0d 0h 0m 0s")
local leaderLabel = statsSection:CreateParagraph("Leaderstats", "Strength: 0\nRebirths: 0\nKills: 0\nBrawls: 0")
local extraLabel = statsSection:CreateParagraph("Extras", "Good Karma: 0\nEvil Karma: 0\nDurability: 0\nAgility: 0\nMuscle King Time: 0")

local function updatePlayerStats()
    -- [your existing stat fetching code]
    local sessionTime = os.time() - sessionStartTime

    sessionLabel:UpdateParagraph("Session Time", formatTime(sessionTime))
    leaderLabel:UpdateParagraph("Leaderstats", string.format(
        "Strength: %s\nRebirths: %s\nKills: %s\nBrawls: %s",
        formatNumber(strength), formatNumber(rebirths), formatNumber(kills), formatNumber(brawls)
    ))
    extraLabel:UpdateParagraph("Extras", string.format(
        "Good Karma: %s\nEvil Karma: %s\nDurability: %s\nAgility: %s\nMuscle King Time: %s",
        formatNumber(gKarma), formatNumber(eKarma), formatNumber(dur), formatNumber(agi), formatNumber(mkt)
    ))
end

RunService.RenderStepped:Connect(updatePlayerStats)

-- Rebirth tab
local rebSection = rebirthTab:CreateSection("Rebirth Controls")
rebSection:CreateToggle("Auto Rebirth (Infinite)", false, function(s)
    states.AutoRebirth = s
    if s then autoRebirth() end
end)

rebSection:CreateTextbox("Target Rebirths", "Enter number", function(text)
    local num = tonumber(text)
    targetRebirths = num and num > 0 and num or 1
end)

rebSection:CreateToggle("Auto Rebirth to Target", false, function(s)
    states.AutoRebirthTarget = s
    if s then
        -- [your target rebirth loop]
    end
end)

-- Credits tab example
local credSection = creditsTab:CreateSection("Info")
credSection:CreateLabel("Invincibles Priv Script – Visual UI Edition")
credSection:CreateLabel("Made with Visual UI Library by VisualRoblox")
credSection:CreateLabel("Updated March 2026")

print("Invincibles Priv Script – now using Visual UI Library (March 2026)")

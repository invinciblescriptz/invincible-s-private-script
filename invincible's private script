-- Invincibles Priv Script – cleaned & safer (March 2026 version)
-- Changed UI to LinoriaLib (popular & feature-rich in 2026)

local Linoria = loadstring(game:HttpGet('https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/Library.lua'))()
local Window = Linoria:CreateWindow("invincible private | Hello " .. game.Players.LocalPlayer.DisplayName, {
    MainColor = Color3.fromRGB(41, 74, 122),
    AccentColor = Color3.fromRGB(0, 170, 255),
    MinimizeKey = Enum.KeyCode.LeftControl,
})

-- Services
local Players             = game:GetService("Players")
local LocalPlayer         = Players.LocalPlayer
local Workspace           = game:GetService("Workspace")
local ReplicatedStorage   = game:GetService("ReplicatedStorage")
local UserInputService    = game:GetService("UserInputService")
local RunService          = game:GetService("RunService")
local VirtualUser         = game:GetService("VirtualUser")

local muscleEvent = LocalPlayer:WaitForChild("muscleEvent", 8) -- still assuming old name; replace if patched

-- Globals & states
getgenv().NexusRunning = true

local states = {
    AutoPushups       = false,
    AutoWeight        = false,
    AutoHandstands    = false,
    FastPunch         = false,
    FastStrength      = false,
    FastRebirth       = false,
    InfiniteJump      = false,
    AntiAfk           = false,
    AutoRebirth       = false,
    AutoRebirthTarget = false,
    KillAura          = false,
    AutoKillSelected  = false,
}

local targetRebirths   = 1
local selectedTarget   = nil
local fastStrengthConn = nil
local jumpConn         = nil
local antiAfkConn      = nil

-- Rock farm globals
getgenv().rockFarmActive         = false
getgenv().selectedRockDurability = 0

-- =============================================================
-- Helpers (unchanged)
-- =============================================================

local function tpTo(pos)
    pcall(function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(pos)
        end
    end)
end

local function safeEquipTool(toolName)
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Humanoid") then return end
        local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
        if tool and tool.Parent ~= char then
            char.Humanoid:EquipTool(tool)
        end
    end)
end

local function startAutoRep(toolName, stateKey)
    task.spawn(function()
        while states[stateKey] and getgenv().NexusRunning do
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    task.wait(1.6) return
                end
                safeEquipTool(toolName)
                if muscleEvent then
                    muscleEvent:FireServer("rep")
                end
            end)
            task.wait(0.15)
        end
        pcall(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild(toolName) then
                char[toolName].Parent = LocalPlayer.Backpack
            end
        end)
    end)
end

-- Fast Strength
local function toggleFastStrength(state)
    states.FastStrength = state
    if state then
        if fastStrengthConn then fastStrengthConn:Disconnect() end
        fastStrengthConn = RunService.Heartbeat:Connect(function()
            pcall(function()
                if not muscleEvent then return end
                muscleEvent:FireServer("rep")
                muscleEvent:FireServer("punch", "rightHand")
                muscleEvent:FireServer("punch", "leftHand")
                local char = LocalPlayer.Character
                if char then
                    local tool = char:FindFirstChildWhichIsA("Tool")
                    if tool then tool:Activate() end
                end
            end)
        end)
    else
        if fastStrengthConn then
            fastStrengthConn:Disconnect()
            fastStrengthConn = nil
        end
    end
end

-- Infinite Jump
local function toggleInfiniteJump(state)
    states.InfiniteJump = state
    if state then
        if jumpConn then return end
        jumpConn = UserInputService.JumpRequest:Connect(function()
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        end)
    else
        if jumpConn then
            jumpConn:Disconnect()
            jumpConn = nil
        end
    end
end

-- Anti-AFK
local function toggleAntiAfk(state)
    states.AntiAfk = state
    if state then
        if antiAfkConn then return end
        antiAfkConn = LocalPlayer.Idled:Connect(function()
            pcall(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        end)
    else
        if antiAfkConn then
            antiAfkConn:Disconnect()
            antiAfkConn = nil
        end
    end
end

-- Kill player
local function killPlayer(targetPlayer)
    pcall(function()
        local myChar = LocalPlayer.Character
        local tChar  = targetPlayer.Character
        if not myChar or not tChar then return end
        local myHRP = myChar:FindFirstChild("HumanoidRootPart")
        local tHRP  = tChar:FindFirstChild("HumanoidRootPart")
        if myHRP and tHRP then
            myHRP.CFrame = tHRP.CFrame * CFrame.new(0, -25, 0)
        end
    end)
end

-- =============================================================
-- Tabs & UI Elements (Linoria style)
-- =============================================================

local Tabs = {
    Main     = Window:AddTab("Main"),
    Farming  = Window:AddTab("Better Farming"),
    Rebirth  = Window:AddTab("Rebirth"),
    Teleport = Window:AddTab("Teleport"),
    Killer   = Window:AddTab("Killer"),
    Misc     = Window:AddTab("Misc")
}

-- Main Tab - Tools
local ToolsSection = Tabs.Main:AddSection("Tools")
ToolsSection:AddToggle("AutoPushups", "Auto Pushups", false, function(s)
    states.AutoPushups = s
    if s then startAutoRep("Pushups", "AutoPushups") end
end)
ToolsSection:AddToggle("AutoWeight", "Auto Weight", false, function(s)
    states.AutoWeight = s
    if s then startAutoRep("Weight", "AutoWeight") end
end)
ToolsSection:AddToggle("AutoHandstands", "Auto Handstands", false, function(s)
    states.AutoHandstands = s
    if s then startAutoRep("Handstands", "AutoHandstands") end
end)

ToolsSection:AddToggle("FastPunch", "Auto Punch (Fast)", false, function(s)
    states.FastPunch = s
    if s then
        task.spawn(function()
            while states.FastPunch and getgenv().NexusRunning do
                pcall(function()
                    if muscleEvent then
                        muscleEvent:FireServer("punch", "rightHand")
                        muscleEvent:FireServer("punch", "leftHand")
                    end
                    local char = LocalPlayer.Character
                    if char then
                        local punch = char:FindFirstChild("Punch") or LocalPlayer.Backpack:FindFirstChild("Punch")
                        if punch and punch.Parent ~= char then
                            char.Humanoid:EquipTool(punch)
                        end
                        if punch and punch.Parent == char then
                            local atk = punch:FindFirstChild("attackTime")
                            if atk and atk:IsA("NumberValue") then atk.Value = 0.01 end
                            punch:Activate()
                        end
                    end
                end)
                task.wait(0.12)
            end
        end)
    end
end)

ToolsSection:AddButton("Anti Lag (Boost FPS)", function()
    local Lighting = game:GetService("Lighting")
    Lighting.GlobalShadows = false
    Lighting.Brightness = 1
    Lighting.ClockTime = 12
    Lighting.FogEnd = 9999
    Lighting.FogStart = 0
    for _, v in pairs(Workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
           v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = false
        end
    end
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
end)

-- Teleport Tab
local TeleportSection = Tabs.Teleport:AddSection("Teleports")
TeleportSection:AddButton("To Spawn / Safe Area", function() tpTo(Vector3.new(-6, 5, -196)) end)
TeleportSection:AddButton("To Secret Area", function() tpTo(Vector3.new(1947, 2, 6191)) end)
TeleportSection:AddButton("To Tiny Area", function() tpTo(Vector3.new(-39, 6, 1886)) end)
TeleportSection:AddButton("To Frozen Area", function() tpTo(Vector3.new(-2623, 6, -409)) end)
TeleportSection:AddButton("To Mythical Area", function() tpTo(Vector3.new(2251, 6, 1073)) end)
TeleportSection:AddButton("To Inferno Area", function() tpTo(Vector3.new(-6759, 6, -1285)) end)
TeleportSection:AddButton("To Legend Area", function() tpTo(Vector3.new(4603, 990, -3898)) end)
TeleportSection:AddButton("To Muscle King Area", function() tpTo(Vector3.new(-8626, 16, -5730)) end)
TeleportSection:AddButton("To Jungle Area", function() tpTo(Vector3.new(-8642, 6, 2381)) end)
TeleportSection:AddButton("To Lava Brawl", function() tpTo(Vector3.new(4471, 119, -8836)) end)
TeleportSection:AddButton("To Desert Brawl", function() tpTo(Vector3.new(960, 17, -7398)) end)
TeleportSection:AddButton("To Beach Brawl", function() tpTo(Vector3.new(-1849, 20, -6335)) end)

-- Rock Farm (in Main Tab)
local RockSection = Tabs.Main:AddSection("Rock Farm")
local function setRockFarm(dur, enable)
    if enable then
        getgenv().selectedRockDurability = dur
        getgenv().rockFarmActive = true
    else
        if getgenv().selectedRockDurability == dur then
            getgenv().rockFarmActive = false
            getgenv().selectedRockDurability = 0
        end
    end
end

RockSection:AddToggle("FarmTiny", "Farm Tiny Island Rock (0)", false, function(s) setRockFarm(0, s) end)
RockSection:AddToggle("FarmStarter", "Farm Starter Island Rock (100)", false, function(s) setRockFarm(100, s) end)
RockSection:AddToggle("FarmLegendBeach", "Farm Legend Beach Rock (5000)", false, function(s) setRockFarm(5000, s) end)
RockSection:AddToggle("FarmFrost", "Farm Frost Gym Rock (150k)", false, function(s) setRockFarm(150000, s) end)
RockSection:AddToggle("FarmMythical", "Farm Mythical Gym Rock (400k)", false, function(s) setRockFarm(400000, s) end)
RockSection:AddToggle("FarmEternal", "Farm Eternal Gym Rock (750k)", false, function(s) setRockFarm(750000, s) end)
RockSection:AddToggle("FarmLegendGym", "Farm Legend Gym Rock (1M)", false, function(s) setRockFarm(1000000, s) end)
RockSection:AddToggle("FarmMuscleKing", "Farm Muscle King Gym Rock (5M)", false, function(s) setRockFarm(5000000, s) end)
RockSection:AddToggle("FarmJungle", "Farm Ancient Jungle Rock (10M)", false, function(s) setRockFarm(10000000, s) end)

RockSection:AddButton("STOP All Rock Farming", function()
    getgenv().rockFarmActive = false
    getgenv().selectedRockDurability = 0
end)

-- Rock farm loop (unchanged, with safety delay)
task.spawn(function()
    while getgenv().NexusRunning do
        if not getgenv().rockFarmActive or getgenv().selectedRockDurability <= 0 then
            task.wait(0.8) continue
        end
        pcall(function()
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                task.wait(1.4) return
            end
            local dur = LocalPlayer:FindFirstChild("Durability")
            if not dur or dur.Value < getgenv().selectedRockDurability then
                task.wait(0.6) return
            end
            safeEquipTool("Punch")
            for _, obj in pairs(Workspace.machinesFolder:GetDescendants()) do
                if obj.Name == "neededDurability" and obj:IsA("IntValue") and obj.Value == getgenv().selectedRockDurability then
                    local rock = obj.Parent:FindFirstChild("Rock")
                    if rock and rock:IsA("BasePart") then
                        if char:FindFirstChild("RightHand") then
                            firetouchinterest(rock, char.RightHand, 0)
                            task.wait(0.05)
                            firetouchinterest(rock, char.RightHand, 1)
                        end
                        if char:FindFirstChild("LeftHand") then
                            firetouchinterest(rock, char.LeftHand, 0)
                            task.wait(0.05)
                            firetouchinterest(rock, char.LeftHand, 1)
                        end
                        if muscleEvent then
                            muscleEvent:FireServer("punch", "leftHand")
                            muscleEvent:FireServer("punch", "rightHand")
                        end
                    end
                end
            end
        end)
        task.wait(0.25)
    end
end)

-- Killer Tab
local KillerSection = Tabs.Killer:AddSection("Killer Features")
KillerSection:AddToggle("KillAura", "Kill Aura (All)", false, function(s)
    states.KillAura = s
    if s then
        task.spawn(function()
            while states.KillAura and getgenv().NexusRunning do
                for _, plr in Players:GetPlayers() do
                    if plr ~= LocalPlayer and plr.Character then
                        killPlayer(plr)
                    end
                end
                task.wait(0.3)
            end
        end)
    end
end)

-- Player dropdown (Linoria uses :AddDropdown)
local playerList = {}
for _, plr in Players:GetPlayers() do
    if plr ~= LocalPlayer then
        table.insert(playerList, plr.Name)
    end
end

local dropdown = Tabs.Killer:AddDropdown("Select Target", playerList, function(val)
    selectedTarget = val
end)

KillerSection:AddToggle("AutoKillSelected", "Auto Kill Selected", false, function(s)
    states.AutoKillSelected = s
    if s and selectedTarget then
        task.spawn(function()
            while states.AutoKillSelected and getgenv().NexusRunning do
                local plr = Players:FindFirstChild(selectedTarget)
                if plr and plr.Character then
                    killPlayer(plr)
                end
                task.wait(0.3)
            end
        end)
    end
end)

-- Update dropdown periodically
task.spawn(function()
    while getgenv().NexusRunning do
        playerList = {}
        for _, plr in Players:GetPlayers() do
            if plr ~= LocalPlayer then
                table.insert(playerList, plr.Name)
            end
        end
        dropdown:Refresh(playerList, true)  -- Linoria method to update values
        task.wait(6)
    end
end)

-- Better Farming Tab
local FarmSection = Tabs.Farming:AddSection("Farming")
FarmSection:AddToggle("FastStrength", "Fast Grind / Strength", false, toggleFastStrength)

FarmSection:AddToggle("FastRebirth", "Fast Rebirth", false, function(s)
    states.FastRebirth = s
    if s then
        task.spawn(function()
            while states.FastRebirth and getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.25)
            end
        end)
    end
end)

-- Misc Tab
local MiscSection = Tabs.Misc:AddSection("Misc")
MiscSection:AddToggle("InfiniteJump", "Infinite Jump", false, toggleInfiniteJump)
MiscSection:AddToggle("AntiAfk", "Anti-AFK", false, toggleAntiAfk)

-- Rebirth Tab
local RebirthSection = Tabs.Rebirth:AddSection("Rebirth Options")
RebirthSection:AddToggle("AutoRebirth", "Auto Rebirth (Infinite)", false, function(s)
    states.AutoRebirth = s
    if s then
        task.spawn(function()
            while states.AutoRebirth and getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end)

RebirthSection:AddTextbox("Target Rebirths", "1", function(value)
    local num = tonumber(value)
    targetRebirths = num and num > 0 and num or 1
end)

RebirthSection:AddToggle("AutoRebirthTarget", "Auto Rebirth to Target", false, function(s)
    states.AutoRebirthTarget = s
    if s then
        task.spawn(function()
            while states.AutoRebirthTarget and getgenv().NexusRunning do
                pcall(function()
                    local rebirths = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Rebirths")
                    if rebirths and rebirths.Value >= targetRebirths then
                        states.AutoRebirthTarget = false
                        return
                    end
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("rebirthRemote") then
                        rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end)

RebirthSection:AddToggle("AutoSize2", "Auto Size 2", false, function(s)
    if s then
        getgenv().sizeLoop = task.spawn(function()
            while getgenv().NexusRunning do
                pcall(function()
                    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
                    if rEvents and rEvents:FindFirstChild("changeSpeedSizeRemote") then
                        rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 2)
                    end
                end)
                task.wait(0.15)
            end
        end)
    else
        if getgenv().sizeLoop then
            task.cancel(getgenv().sizeLoop)
            getgenv().sizeLoop = nil
        end
    end
end)

RebirthSection:AddToggle("AutoTPMuscleKing", "Auto TP Muscle King", false, function(s)
    if s then
        getgenv().kingTpLoop = task.spawn(function()
            while getgenv().NexusRunning do
                tpTo(Vector3.new(-8646, 17, -5738))
                task.wait(0.25)
            end
        end)
    else
        if getgenv().kingTpLoop then
            task.cancel(getgenv().kingTpLoop)
            getgenv().kingTpLoop = nil
        end
    end
end)

print("Invincibles Priv Script – Linoria UI (March 2026)")

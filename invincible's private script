local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library", true))()
local window = library:AddWindow("Invincibles Priv Script", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size = Vector2.new(470, 470),
    can_resize = false,
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")

local main = window:AddTab("Main")
local Tools = main:AddFolder("Tools")
local Rocks = main:AddFolder("Rocks")

-- Helper: Auto-rep loop (training tools)
local function startAutoRep(toolName, toggleVar)
    task.spawn(function()
        while _G[toggleVar] do
            pcall(function()
                local character = LocalPlayer.Character
                if not character or not character:FindFirstChild("Humanoid") or character.Humanoid.Health <= 0 then
                    task.wait(1.5)
                    return
                end
                local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or character:FindFirstChild(toolName)
                if tool and tool.Parent ~= character then
                    character.Humanoid:EquipTool(tool)
                end
                local muscleEvent = LocalPlayer:FindFirstChild("muscleEvent")
                if muscleEvent then
                    muscleEvent:FireServer("rep")
                end
            end)
            task.wait(0.025)
        end
        -- Unequip on disable
        pcall(function()
            local character = LocalPlayer.Character
            if character then
                local equipped = character:FindFirstChild(toolName)
                if equipped then
                    equipped.Parent = LocalPlayer.Backpack
                end
            end
        end)
    end)
end

-- Auto training switches
Tools:AddSwitch("Auto Pushups", function(state)
    _G.AutoPushups = state
    if state then startAutoRep("Pushups", "AutoPushups") end
end)
Tools:AddSwitch("Auto Weight", function(state)
    _G.AutoWeight = state
    if state then startAutoRep("Weight", "AutoWeight") end
end)
Tools:AddSwitch("Auto Handstands", function(state)
    _G.AutoHandstands = state
    if state then startAutoRep("Handstands", "AutoHandstands") end
end)

-- Fast Punch
Tools:AddSwitch("Fast Punch", function(state)
    _G.FastPunch = state
    if state then
        task.spawn(function()
            while _G.FastPunch do
                pcall(function()
                    local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch"))
                    if punch and punch:FindFirstChild("attackTime") then
                        punch.attackTime.Value = 0
                    end
                end)
                task.wait(0.25)
            end
        end)
        task.spawn(function()
            while _G.FastPunch do
                pcall(function()
                    local muscleEvent = LocalPlayer:FindFirstChild("muscleEvent")
                    if muscleEvent then
                        muscleEvent:FireServer("punch", "rightHand")
                        muscleEvent:FireServer("punch", "leftHand")
                    end
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("Punch") then
                        char.Punch:Activate()
                    end
                end)
                task.wait(0.07)
            end
        end)
    end
end)

-- Anti-Lag
Tools:AddButton("Anti Lag (Boost FPS)", function()
    local Lighting = game:GetService("Lighting")
    Lighting.GlobalShadows = false
    Lighting.Brightness = 1
    Lighting.ClockTime = 12
    Lighting.FogEnd = 9999
    Lighting.FogStart = 0
    for _, v in pairs(Workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
           v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = false
        end
    end
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    print("Anti-Lag activated → check FPS (Shift+F5)")
end)

-- Rock farm helper
local function startRockFarm(toggleVar, rockNameFilter, requiredDur)
    print("Starting rock farm for dur >= " .. requiredDur .. " | Filter: " .. (rockNameFilter ~= "" and rockNameFilter or "Any") .. " | WARNING: Use at own risk!")
    task.spawn(function()
        while _G[toggleVar] do
            pcall(function()
                local char = LocalPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    task.wait(1)
                    return
                end
                if LocalPlayer:FindFirstChild("Durability") and LocalPlayer.Durability.Value >= requiredDur then
                    for _, v in pairs(Workspace.machinesFolder:GetDescendants()) do
                        if v.Name == "neededDurability" and v.Value == requiredDur then
                            local rockPart = v.Parent:FindFirstChildWhichIsA("BasePart") or v.Parent -- improved fallback
                            if rockPart and rockPart:IsA("BasePart") then
                                local parentName = rockPart.Parent.Name
                                local partName = rockPart.Name
                                if rockNameFilter == "" or string.find(parentName:lower(), rockNameFilter:lower()) or string.find(partName:lower(), rockNameFilter:lower()) then
                                    firetouchinterest(rockPart, char:FindFirstChild("RightHand"), 0)
                                    task.wait(0.01)
                                    firetouchinterest(rockPart, char:FindFirstChild("RightHand"), 1)
                                    firetouchinterest(rockPart, char:FindFirstChild("LeftHand"), 0)
                                    task.wait(0.01)
                                    firetouchinterest(rockPart, char:FindFirstChild("LeftHand"), 1)
                                end
                            end
                        end
                    end
                end
                gettool()  -- punch after touch attempt
            end)
            task.wait(0.15)  -- safer timing
        end
    end)
end

-- Improved gettool (equip only if needed)
function gettool()
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Humanoid") then return end
        local alreadyEquipped = char:FindFirstChild("Punch")
        if not alreadyEquipped then
            for _, v in pairs(LocalPlayer.Backpack:GetChildren()) do
                if v.Name == "Punch" then
                    char.Humanoid:EquipTool(v)
                    break
                end
            end
        end
        local muscleEvent = LocalPlayer:FindFirstChild("muscleEvent")
        if muscleEvent then
            muscleEvent:FireServer("punch", "leftHand")
            muscleEvent:FireServer("punch", "rightHand")
        end
    end)
end

-- Rock switches (enable one at a time recommended)
Rocks:AddSwitch("Farm Tiny Island Rock (0 Dur)", function(state)
    _G.FarmTiny = state
    if state then startRockFarm("FarmTiny", "tiny", 0) end
end)

Rocks:AddSwitch("Farm Starter/Large Rock (100 Dur)", function(state)
    _G.FarmStarter = state
    if state then startRockFarm("FarmStarter", "starter", 100) end
end)

Rocks:AddSwitch("Farm Legend Beach/Golden (5k Dur)", function(state)
    _G.FarmLegendBeach = state
    if state then startRockFarm("FarmLegendBeach", "legend", 5000) end
end)

Rocks:AddSwitch("Farm Frost Gym Rock (150k Dur)", function(state)
    _G.FarmFrost = state
    if state then startRockFarm("FarmFrost", "frost", 150000) end
end)

Rocks:AddSwitch("Farm Mythical Gym Rock (400k Dur)", function(state)
    _G.FarmMythical = state
    if state then startRockFarm("FarmMythical", "mythical", 400000) end
end)

Rocks:AddSwitch("Farm Ancient Jungle Rock (10M Dur)", function(state)
    _G.FarmAncientJungle = state
    if state then startRockFarm("FarmAncientJungle", "ancient jungle", 10000000) end
end)

Rocks:AddSwitch("Farm Any 10M+ Rock (No Name Filter)", function(state)
    _G.FarmAny10M = state
    if state then startRockFarm("FarmAny10M", "", 10000000) end
end)

-- Teleport Tab (unchanged, but used Vector3 for clarity)
local Teleport = window:AddTab("Teleport")
local function tpTo(pos)
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.CFrame = CFrame.new(pos) end
end

Teleport:AddButton("To Spawn / Safe Area", function() tpTo(Vector3.new(2, 8, 115)) end)
Teleport:AddButton("To Secret Area", function() tpTo(Vector3.new(1947, 2, 6191)) end)
Teleport:AddButton("To Tiny Area", function() tpTo(Vector3.new(-34, 7, 1903)) end)
Teleport:AddButton("To Frozen Area", function() tpTo(Vector3.new(-2600.00244, 3.67686558, -403.884369)) end)
Teleport:AddButton("To Mythical Area", function() tpTo(Vector3.new(2255, 7, 1071)) end)
Teleport:AddButton("To Inferno Area", function() tpTo(Vector3.new(-6768, 7, -1287)) end)
Teleport:AddButton("To Legend Area", function() tpTo(Vector3.new(4604, 991, -3887)) end)
Teleport:AddButton("To Muscle King Area", function() tpTo(Vector3.new(-8646, 17, -5738)) end)
Teleport:AddButton("To Jungle Area", function() tpTo(Vector3.new(-8659, 6, 2384)) end)
Teleport:AddButton("To Lava Brawl", function() tpTo(Vector3.new(4471, 119, -8836)) end)
Teleport:AddButton("To Desert Brawl", function() tpTo(Vector3.new(960, 17, -7398)) end)
Teleport:AddButton("To Beach Brawl", function() tpTo(Vector3.new(-1849, 20, -6335)) end)

-- Kill Tab (unchanged - functional but risky)
local KillTab = window:AddTab("Kill")
local KillFolder = KillTab:AddFolder("Auto Kill")

local function checkCharacter()
    if not LocalPlayer.Character then LocalPlayer.CharacterAdded:Wait() end
    return LocalPlayer.Character
end

local function killPlayer(targetPlayer)
    local char = checkCharacter()
    if not char or not char:FindFirstChild("LeftHand") then return end
    if targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") and
       targetPlayer.Character:FindFirstChild("Humanoid") and targetPlayer.Character.Humanoid.Health > 0 then
        firetouchinterest(targetPlayer.Character.HumanoidRootPart, char.LeftHand, 0)
        firetouchinterest(targetPlayer.Character.HumanoidRootPart, char.LeftHand, 1)
        local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or char:FindFirstChild("Punch")
        if punch and char:FindFirstChild("Humanoid") then
            char.Humanoid:EquipTool(punch)
        end
        local muscleEvent = LocalPlayer:FindFirstChild("muscleEvent")
        if muscleEvent then
            muscleEvent:FireServer("punch", "leftHand")
            muscleEvent:FireServer("punch", "rightHand")
        end
    end
end

KillFolder:AddSwitch("Kill All Players (Aura)", function(state)
    getgenv().killAura = state
    if state then
        task.spawn(function()
            while getgenv().killAura do
                pcall(function()
                    for _, plr in ipairs(Players:GetPlayers()) do
                        if plr ~= LocalPlayer and plr.Character then
                            killPlayer(plr)
                        end
                    end
                end)
                task.wait(0.08)
            end
        end)
    end
end)

-- Single target (dropdown + switch unchanged)
local targetKillPlayer = nil
local playerDropdown = KillFolder:AddDropdown("Select Target Player", {
    Title = "Single Target Kill",
    Values = {},
    Callback = function(selected)
        local name = selected:match("^([^|]+)") or selected
        targetKillPlayer = name
        print("Kill target set to: " .. name)
    end
})

task.spawn(function()
    while true do
        local list = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer then
                table.insert(list, plr.Name .. " | " .. plr.DisplayName)
            end
        end
        playerDropdown:SetValues(list)
        task.wait(5)
    end

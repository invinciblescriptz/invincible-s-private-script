local ScriptLibrary = loadstring(game:HttpGet("https://raw.githubusercontent.com/LightMildTH/EleriumUILibrary/refs/heads/master/EleriumLibrary.lua", true))()

local playerName = game.Players.LocalPlayer.DisplayName
local windowTitle = string.format("invincible v2|| Private || Hello %s", playerName)

local MainWindow = ScriptLibrary:AddWindow(windowTitle, {
    min_size = Vector2.new(660, 700),
    can_resize = true,
    main_color = Color3.fromRGB(0, o, 0)
})

